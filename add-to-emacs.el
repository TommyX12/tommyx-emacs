
;; pre-load configurations
(setq org-agenda-dir "~/notes/agenda") ; remove this if you are not me.

(load "~/tommyx-emacs/tommyx.el")

;; post-load configurations
; add things to org agenda files
(append 'org-agenda-files '())
