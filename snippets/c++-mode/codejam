# -*- mode: snippet -*-
# name: codejam
# key: codejam
# --
#include <cstdio>

using namespace std;

int T;

int main() {
#ifdef DEBUG
    freopen("${1:`(file-name-base (buffer-file-name))`}.txt", "r", stdin);
#endif

	scanf("%d", &T);
    for (int t = 0; t < T; ++t) {
       $0
        printf("Case #%d: \n", t + 1);
    }
}
